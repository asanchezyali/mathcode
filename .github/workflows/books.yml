name: Build LaTeX Books
on: [push]
jobs:
  build_latex_books:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    strategy:
      max-parallel: 1  # Compile one at a time to avoid conflicts
      matrix:
        book:
          - name: deep_learning
            dir: deep_learning
            pdf: deep_learning.pdf
            emoji: "🧠"
          - name: machine_learning
            dir: machine_learning
            pdf: machine_learning.pdf
            emoji: "🤖"
          - name: llms
            dir: llms
            pdf: llms.pdf
            emoji: "💬"
          - name: inferencial_statistics
            dir: inferencial_statistics
            pdf: inferencial_statistics.pdf
            emoji: "📊"
          - name: src
            dir: src
            pdf: src.pdf
            emoji: "📚"
    steps:
      - name: Set up Git repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check for changes in ${{ matrix.book.name }}
        id: check_changes
        run: |
          # Check if book directory or workflow changed
          if git diff --name-only ${{ github.event.before }} ${{ github.sha }} | grep -qE '^(${{ matrix.book.dir }}/|\.github/workflows/)'; then
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "Book ${{ matrix.book.name }} or workflow has changes"
          else
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "Book ${{ matrix.book.name }} has no changes"
          fi

      - name: Compile ${{ matrix.book.name }}
        if: steps.check_changes.outputs.changed == 'true'
        uses: xu-cheng/latex-action@v3
        with:
          working_directory: ${{ matrix.book.dir }}
          root_file: main.tex
          args: -pdf -file-line-error -interaction=nonstopmode -shell-escape

      - name: Rename PDF
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          if [ -f "${{ matrix.book.dir }}/main.pdf" ]; then
            mv "${{ matrix.book.dir }}/main.pdf" "${{ matrix.book.dir }}/${{ matrix.book.pdf }}"
          fi

      - name: Get current date
        id: date
        if: steps.check_changes.outputs.changed == 'true'
        run: echo "date=$(date +'%Y-%m-%d %H:%M:%S')" >> $GITHUB_OUTPUT

      - name: Upload PDF to pdfs branch
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"

          # Verify PDF exists
          if [ ! -f "${{ matrix.book.dir }}/${{ matrix.book.pdf }}" ]; then
            echo "ERROR: PDF not found at ${{ matrix.book.dir }}/${{ matrix.book.pdf }}"
            exit 1
          fi

          # Create temporary directory and copy ONLY the PDF
          mkdir -p /tmp/pdf_upload
          cp "${{ matrix.book.dir }}/${{ matrix.book.pdf }}" /tmp/pdf_upload/

          # Switch to pdfs branch
          if git show-ref --verify --quiet refs/remotes/origin/pdfs; then
            git fetch origin pdfs
            git checkout pdfs
            git pull --rebase origin pdfs || true
          else
            git checkout --orphan pdfs
            git rm -rf . || true
          fi

          # Remove all files except .git directory
          find . -maxdepth 1 -not -name '.git' -not -name '.' -delete 2>/dev/null || true

          # Copy only the PDF file
          cp /tmp/pdf_upload/${{ matrix.book.pdf }} .

          # Add and commit only if changed
          git add ${{ matrix.book.pdf }}
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "${{ matrix.book.emoji }} Update ${{ matrix.book.pdf }} - ${{ steps.date.outputs.date }}"

            # Push with retry
            for i in 1 2 3; do
              if git push origin pdfs; then
                echo "Push successful!"
                break
              fi
              echo "Push failed, retrying in 5 seconds..."
              sleep 5
              git pull --rebase origin pdfs
            done
          fi

          # Switch back to main branch
          git checkout ${{ github.ref_name }}

      - name: Upload PDF as artifact
        if: steps.check_changes.outputs.changed == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.book.name }}-pdf
          path: "${{ matrix.book.dir }}/${{ matrix.book.pdf }}"
          if-no-files-found: warn
